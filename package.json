{
  "name": "miltons-properties",
  "version": "0.1.0",
  "private": true,
  "description": "Milton's Properties — owner/realtor/renter platform. pnpm monorepo: iOS shell, web app, API, shared types.",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/web dev",
    "dev:api": "pnpm --filter @workspace/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  }
}
